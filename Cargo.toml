[package]
name = "block-graph"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.18", features = ["fusion","ndarray","train"]}
rand = "0.9"
serde = { version = "1", features = ["derive"]}
[profile.test]
opt-level = 3
