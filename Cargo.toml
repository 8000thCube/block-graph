[package]
name = "block-graph"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.18", features = ["fusion","ndarray","train","wgpu"]}
rand = "0.9"

[profile.test]
opt-level = 3
